import scala.collection.mutable.ArrayBuffer
/*
    Acts like a basic SQL table to a certain extent.
    Missing functionality such as dropping tables because...
    I don't hate myself and that will make things much safer (hopefully)
*/
class Table(file: String) // File to read from and write to at the end
{
    ArrayBuffer[]

    def readFile = 
    {

    }

    def writeFile = 
    {

    }

    def getEntry(key: String | Int): Option[Array[Int | Double | String]] = 
    {
        try {

        }
        catch {
            null
        }
    }

}